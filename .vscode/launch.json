{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "coreclr",
      "request": "launch",
      "name": "Launch .NET Core App",
      "program": "${workspaceFolder}/${input:programPath}",
      "preLaunchTask": "build"
    }
  ],
  "inputs": [
    {
      "type": "promptString",
      "id": "programPath",
      "description": "Enter the path to your .NET executable (e.g., bin/Debug/net8.0/CarAppUpd.dll)"
    }
  ]
}